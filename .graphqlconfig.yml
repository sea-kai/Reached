projects:
  OpenSpaceAPI:
    schemaPath: src/graphql/schema.json
    includes:
      - OpenSpace.ts
    excludes:
      - ./amplify/**
    extensions:
      amplify:
        codeGenTarget: typescript
        generatedFileName: src/API.ts
        docsFilePath: .
extensions:
  amplify:
    version: 3
